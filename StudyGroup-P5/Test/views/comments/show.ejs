<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Comments</title>
    <style>
        * {
            margin: 0;
            padding: 0;
        }
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        a {
            color: black;
            text-decoration: none;
        }
        #deleteBtn {
            display: inline;
            float: right;
            cursor: pointer;
            border: none;
            color: black;
            background-color: white;
        }
        #edit {
            float: left;
        }
        #back {
            padding: 5px 10px;
            transition: 0.2s;
            cursor: pointer;
            border: none;
            color: white;
            background-color: rgb(125, 125, 125);
        }
        #back:hover {
            background-color: rgb(90, 90, 90);
        }
        section {
            max-width: 400px;
        }
    </style>
</head>
<body>
    <section>
        <a href="/comments"><button id="back">Kembali</button></a>
        <h2><%= comment.comment %> - <%= comment.username %></h2>
        <a id="edit" href="/comments/edit/<%= comment.id %>">Edit</a>
        <form action="/comments/<%= comment.id %>?_method=DELETE" method="post"><button id="deleteBtn">Delete</button></form>
    </section>
</body>
</html>