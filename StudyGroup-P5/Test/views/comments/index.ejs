<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comments</title>
    <style>
        * {
            margin: 0;
            padding: 0;
        }
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        button {
            padding: 5px 10px;
            transition: 0.2s;
            cursor: pointer;
            border: none;
            color: white;
            background-color: rgb(125, 125, 125);
        }
        button:hover {
            background-color: rgb(90, 90, 90);
        }
        section {
            max-width: 400px;
        }
        ul {
            padding-left: 0;
        }
        ul li {
            list-style: none;
            display: flex;
            align-items: flex-start;
            gap: 6px;
            margin-bottom: 8px;
        }
        ul li::before {
            content: "🗨️";
            flex-shrink: 0;
        }
    </style>
</head>
<body>
    <section>
        <a href="/comments/create"><button>Tambah</button></a>
        <h1>List of Comments</h1>
        <hr>
        <ul>
            <% if (comments.length === 0) { %>
                <p>Belum ada komentar</p>
            <% } else { %>
                <% for (let comment of comments) { %>
                <li><a href="/comments/<%= comment.id %>"><%= comment.comment %></a> - <%= comment.username %></li>
                <% } %>
            <% } %>
        </ul>
    </section>
</body>
</html>